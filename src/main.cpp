#include "../headers/restaurants.hpp"
#include "../headers/menu.hpp"
#include <iostream>
#include <vector>
#include <algorithm>

using std::cin;
using std::cout;
using std::endl;
using std::string;

int main() {
  string choice;
  choice = menuDisplay();
  return 0;
}